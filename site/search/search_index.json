{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":""},{"location":"#isidetector","title":"\ud83d\udce6 IsiDetector","text":"<p>   A modular, config-driven instance segmentation pipeline for industrial parcel detection.      Switch between YOLOv12-seg and RF-DETR-Seg with one line of YAML. </p> <p>Get Started  Architecture </p>"},{"location":"#why-isidetector","title":"Why IsiDetector?","text":"<p>IsiDetector is built for logistics and warehouse environments where you need to detect and segment parcels \u2014 specifically cartons and polybags \u2014 on conveyor belts under challenging industrial lighting.</p>"},{"location":"#two-architectures-one-interface","title":"\ud83e\udde0 Two Architectures, One Interface","text":"<p>Train with a CNN-based YOLOv12-seg for speed, or a Transformer-based RF-DETR-Seg (DINOv2 backbone) for global context. Both share the exact same API.</p>"},{"location":"#config-driven-everything","title":"\u2699\ufe0f Config-Driven Everything","text":"<p>No code changes needed. Your model architecture, optimizer, learning rate schedule, augmentation strategy, and early stopping rules are all in YAML.</p>"},{"location":"#plugin-hook-system","title":"\ud83d\udd0c Plugin Hook System","text":"<p>Attach loggers, alerters, or metric trackers by name in your config. Write a class, register it, add one line to YAML \u2014 done.</p>"},{"location":"#industrial-preprocessing","title":"\ud83c\udfed Industrial Preprocessing","text":"<p>Built-in CLAHE-based <code>SpecularGuard</code> handles polybag glare and deep shadows in LAB colour space without distorting the actual parcel colours.</p>"},{"location":"#the-pipeline-at-a-glance","title":"The Pipeline at a Glance","text":"\ud83d\udcc4 train.yaml \u2192 \ud83d\ude80 run_train.py \u2192 \ud83d\udd0d Registry Lookup \u2192 \ud83d\udd28 Trainer.train() \u2192 \ud83d\udcca Trainer.evaluate() \u2192 \ud83d\udce6 Trainer.export()"},{"location":"#project-structure","title":"Project Structure","text":"<pre><code>logistic/\n\u251c\u2500\u2500 isitec_app/                       # \ud83c\udf10 VisionAI Platform (Flask)\n\u2502   \u251c\u2500\u2500 app.py                        # Backend entrypoint\n\u2502   \u251c\u2500\u2500 stream_handler.py             # Async inference management\n\u2502   \u251c\u2500\u2500 templates/                    # Material UI index\n\u2502   \u2514\u2500\u2500 static/                       # CSS/JS &amp; Assets\n\u251c\u2500\u2500 configs/                          # \u2699\ufe0f YAML configurations\n\u2502   \u251c\u2500\u2500 train.yaml                    # Master switchboard\n\u2502   \u2514\u2500\u2500 optimizers/                   # Hyperparameter presets\n\u251c\u2500\u2500 src/                              # \ud83c\udfd7\ufe0f Core Engine\n\u2502   \u251c\u2500\u2500 shared/registry.py            # Global module registry\n\u2502   \u251c\u2500\u2500 training/                     # Training logic\n\u2502   \u2502   \u251c\u2500\u2500 trainers/                 # YOLOv12 &amp; RF-DETR implementations\n\u2502   \u2502   \u2514\u2500\u2500 hooks/                    # Logging &amp; Metrics system\n\u2502   \u2514\u2500\u2500 inference/                    # High-speed ONNX &amp; Torch engines\n\u251c\u2500\u2500 models/                           # \ud83e\udde0 Pretrained &amp; Exported weights\n\u251c\u2500\u2500 data/                             # \ud83d\udcca Training &amp; Validation sets\n\u251c\u2500\u2500 docs/                             # \ud83d\udcd1 Documentation source\n\u2514\u2500\u2500 site/                             # \ud83c\udf0d Built MkDocs static site\n</code></pre>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This guide walks you through setting up IsiDetector and running your first training session.</p>"},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.10+</li> <li>CUDA-capable GPU with at least 8 GB VRAM (12 GB recommended for RF-DETR)</li> <li>pip for package management</li> </ul>"},{"location":"getting-started/#installation","title":"Installation","text":""},{"location":"getting-started/#1-clone-the-repository","title":"1. Clone the Repository","text":"<pre><code>git clone https://github.com/waledroid/IsiDetector.git\ncd IsiDetector\n</code></pre>"},{"location":"getting-started/#2-install-dependencies","title":"2. Install Dependencies","text":"For YOLO TrainingFor RF-DETR TrainingFor Everything <pre><code>pip install ultralytics supervision opencv-python pyyaml\n</code></pre> <pre><code>pip install rfdetr supervision opencv-python pyyaml pandas matplotlib\n</code></pre> <pre><code>pip install ultralytics rfdetr supervision opencv-python pyyaml pandas matplotlib\n</code></pre>"},{"location":"getting-started/#dataset-setup","title":"Dataset Setup","text":""},{"location":"getting-started/#yolo-format-for-yolov12-seg","title":"YOLO Format (for YOLOv12-seg)","text":"<p>Your dataset should follow the standard YOLO segmentation layout:</p> <pre><code>data/isi_3k_dataset/\n\u251c\u2500\u2500 images/\n\u2502   \u251c\u2500\u2500 train/          # Training images (.jpg)\n\u2502   \u251c\u2500\u2500 val/            # Validation images\n\u2502   \u2514\u2500\u2500 test/           # Test images\n\u2514\u2500\u2500 labels/\n    \u251c\u2500\u2500 train/          # YOLO polygon labels (.txt)\n    \u251c\u2500\u2500 val/\n    \u2514\u2500\u2500 test/\n</code></pre> <p>Auto-generated data.yaml</p> <p>You don't need to create <code>data.yaml</code> yourself. The <code>YOLOTrainer</code> will automatically generate it from your <code>train.yaml</code> config using the <code>nc</code> and <code>class_names</code> fields.</p>"},{"location":"getting-started/#coco-format-for-rf-detr","title":"COCO Format (for RF-DETR)","text":"<p>RF-DETR expects COCO-style annotations:</p> <pre><code>data/rfdetr_dataset/\n\u251c\u2500\u2500 train/\n\u2502   \u251c\u2500\u2500 images/         # Training images\n\u2502   \u2514\u2500\u2500 _annotations.coco.json\n\u251c\u2500\u2500 valid/\n\u2502   \u251c\u2500\u2500 images/\n\u2502   \u2514\u2500\u2500 _annotations.coco.json\n\u2514\u2500\u2500 test/\n    \u251c\u2500\u2500 images/\n    \u2514\u2500\u2500 _annotations.coco.json\n</code></pre> <p>Converting Formats</p> <p>Use <code>scripts/prep_rfdetr_data.py</code> to convert your existing YOLO-format dataset into COCO format for RF-DETR.</p>"},{"location":"getting-started/#your-first-training-run","title":"Your First Training Run","text":""},{"location":"getting-started/#step-1-choose-your-model","title":"Step 1: Choose Your Model","text":"<p>Open <code>configs/train.yaml</code> and uncomment the engine block you want:</p>  YOLO (Fast, CNN-based) RF-DETR (Transformer, DINOv2) <pre><code>model_type: \"yolo\"\nmodel_size: \"m\"\noptimizer_config: \"configs/optimizers/yolo_optim.yaml\"\ndataset_path: \"data/isi_3k_dataset\"\nbatch_size: 16\nimage_size: 640\n</code></pre> <pre><code>model_type: \"rfdetr\"\nmodel_size: \"m\"\noptimizer_config: \"configs/optimizers/rfdetr_optim.yaml\"\ndataset_path: \"data/rfdetr_dataset\"\nbatch_size: 4\nimage_size: 640\n</code></pre>"},{"location":"getting-started/#step-2-launch-training","title":"Step 2: Launch Training","text":"<pre><code>python scripts/run_train.py\n</code></pre> <p>That's it. The pipeline will:</p> <ol> <li>Load your YAML config</li> <li>Merge the optimizer config</li> <li>Look up the trainer from the registry</li> <li>Run the full train \u2192 evaluate \u2192 export cycle</li> </ol>"},{"location":"getting-started/#step-3-resume-interrupted-training-yolo-only","title":"Step 3: Resume Interrupted Training (YOLO only)","text":"<pre><code>python scripts/run_train.py --resume models/yolo/24-03-2026/weights/last.pt\n</code></pre>"},{"location":"getting-started/#what-happens-under-the-hood","title":"What Happens Under the Hood","text":"<pre><code>sequenceDiagram\n    participant CLI as run_train.py\n    participant YAML as train.yaml\n    participant REG as Registry\n    participant T as Trainer\n    participant H as Hooks\n\n    CLI-&gt;&gt;YAML: Load config\n    CLI-&gt;&gt;YAML: Merge optimizer YAML\n    CLI-&gt;&gt;REG: TRAINERS.get(model_type)\n    REG--&gt;&gt;CLI: TrainerClass\n    CLI-&gt;&gt;T: TrainerClass(config)\n    T-&gt;&gt;H: Instantiate hooks from config\n    CLI-&gt;&gt;T: trainer.train()\n    T-&gt;&gt;H: call_hooks('before_train')\n    loop Each Epoch\n        T-&gt;&gt;H: call_hooks('after_epoch')\n    end\n    T-&gt;&gt;H: call_hooks('after_train')\n    CLI-&gt;&gt;T: trainer.evaluate()\n    CLI-&gt;&gt;T: trainer.export('onnx')</code></pre>"},{"location":"getting-started/#next-steps","title":"Next Steps","text":"<ul> <li> Read the Architecture Overview to understand the design</li> <li> Explore the Configuration Guide to tune every parameter</li> <li> Learn about the Hook System to add custom logging</li> </ul>"},{"location":"architecture/","title":"Architecture","text":"<p>Understand how IsiDetector's layered, modular design works \u2014 from the Registry pattern to the BaseTrainer contract.</p>"},{"location":"architecture/#system-overview","title":"\ud83d\udd0d System Overview","text":"<p>Full architecture diagram, the five-layer design, and end-to-end data flow.</p> <p> Read More</p>"},{"location":"architecture/#registry-pattern","title":"\ud83e\udde9 Registry Pattern","text":"<p>How the <code>@TRAINERS.register()</code> decorator enables zero-modification extensibility.</p> <p> Read More</p>"},{"location":"architecture/#basetrainer-contract","title":"\ud83d\udcdc BaseTrainer Contract","text":"<p>The abstract foundation that enforces <code>build_model</code>, <code>train</code>, <code>evaluate</code>, and <code>export</code>.</p> <p> Read More</p>"},{"location":"architecture/base-trainer/","title":"The BaseTrainer Contract","text":"<p><code>BaseTrainer</code> is the abstract foundation that every training engine in IsiDetector must inherit from. It defines the universal interface, manages output directories, and drives the hook lifecycle.</p>"},{"location":"architecture/base-trainer/#why-an-abstract-base","title":"Why an Abstract Base?","text":"<p>The <code>BaseTrainer</code> solves a critical problem: the orchestration layer (<code>run_train.py</code>) needs to call <code>.train()</code>, <code>.evaluate()</code>, and <code>.export()</code> without knowing or caring which model is underneath.</p> <pre><code>classDiagram\n    class BaseTrainer {\n        &lt;&lt;abstract&gt;&gt;\n        +config: dict\n        +model_name: str\n        +output_dir: Path\n        +model: Any\n        +current_epoch: int\n        +current_loss: float\n        +hooks: list\n        +call_hooks(stage: str)\n        +build_model()*\n        +train()*\n        +evaluate()* dict\n        +export(format: str)*\n    }\n\n    class YOLOTrainer {\n        +model_size: str\n        +dataset_path: Path\n        +data_yaml_path: str\n        +build_model()\n        +train()\n        +evaluate() dict\n        +export(format: str)\n    }\n\n    class RFDETRTrainer {\n        +model_size: str\n        +dataset_path: Path\n        +history: list\n        +build_model()\n        +train()\n        +evaluate() dict\n        +export(format: str)\n    }\n\n    BaseTrainer &lt;|-- YOLOTrainer\n    BaseTrainer &lt;|-- RFDETRTrainer</code></pre>"},{"location":"architecture/base-trainer/#annotated-source","title":"Annotated Source","text":"<p> Source: <code>src/training/base_trainer.py</code></p>"},{"location":"architecture/base-trainer/#constructor","title":"Constructor","text":"<pre><code>class BaseTrainer(ABC):\n    def __init__(self, config: dict):\n        self.config = config\n        self.model_name = config.get('model_type', 'unknown_model')  # (1)!\n\n        # 1. Setup Output Directories\n        self.output_dir = Path(config.get('output_dir', 'models')) / self.model_name  # (2)!\n        self.output_dir.mkdir(parents=True, exist_ok=True)\n\n        # 2. Universal State Variables\n        self.model = None          # (3)!\n        self.current_epoch = 0\n        self.current_loss = 0.0\n\n        # 3. Initialize Hooks from config\n        self.hooks = []\n        hook_names = config.get('hooks', [])  # (4)!\n        for h_name in hook_names:\n            try:\n                hook_class = HOOKS.get(h_name)\n                self.hooks.append(hook_class())\n            except KeyError:\n                logger.error(f\"\u274c Hook '{h_name}' not found in Registry.\")\n</code></pre> <ol> <li>Reads <code>model_type</code> from config to use as a folder name (e.g., <code>\"yolo\"</code> or <code>\"rfdetr\"</code>)</li> <li>Default output root is <code>models/</code>. Creates <code>models/yolo/</code> or <code>models/rfdetr/</code> automatically</li> <li>These state variables are updated by concrete trainers during training. Hooks read them to display metrics</li> <li>Reads the <code>hooks:</code> list from YAML, looks each one up in the <code>HOOKS</code> registry, and instantiates it</li> </ol>"},{"location":"architecture/base-trainer/#hook-lifecycle","title":"Hook Lifecycle","text":"<pre><code>def call_hooks(self, stage: str):\n    \"\"\"\n    Broadcasts the current stage to all registered hooks.\n    Stages: 'before_train', 'before_epoch', 'after_epoch', 'after_train'\n    \"\"\"\n    for hook in self.hooks:\n        if hasattr(hook, stage):           # (1)!\n            hook_method = getattr(hook, stage)\n            hook_method(self)              # (2)!\n</code></pre> <ol> <li>Checks if the hook implements this particular stage. Hooks are free to only implement the stages they care about</li> <li>Passes <code>self</code> (the trainer instance) to the hook, so hooks can read <code>trainer.current_epoch</code>, <code>trainer.current_loss</code>, <code>trainer.config</code>, etc.</li> </ol> <p>Broadcasting Pattern</p> <p>This is a lightweight Observer pattern. Concrete trainers call <code>self.call_hooks('after_epoch')</code> at the right moment, and all hooks that support that stage get notified.</p>"},{"location":"architecture/base-trainer/#the-four-abstract-methods","title":"The Four Abstract Methods","text":"<pre><code>@abstractmethod\ndef build_model(self):\n    \"\"\"Initializes the architecture and loads it to the GPU.\"\"\"\n    pass\n\n@abstractmethod\ndef train(self):\n    \"\"\"The main training loop. MUST call self.call_hooks() at appropriate stages.\"\"\"\n    pass\n\n@abstractmethod\ndef evaluate(self) -&gt; dict:\n    \"\"\"Runs validation and returns a dictionary of metrics (mAP, etc).\"\"\"\n    pass\n\n@abstractmethod\ndef export(self, format: str = 'onnx'):\n    \"\"\"Exports the trained weights to the deployment format.\"\"\"\n    pass\n</code></pre> Method Must Return Used By <code>build_model()</code> Nothing Called internally by <code>train()</code> if <code>self.model</code> is None <code>train()</code> Nothing Runs the full training loop with hook calls <code>evaluate()</code> <code>dict</code> of metrics Post-training validation: mAP, speed, etc. <code>export()</code> Export file path Converts weights to ONNX/OpenVINO for deployment <p>Contract Rule</p> <p>Every concrete trainer must call <code>self.call_hooks('before_train')</code> and <code>self.call_hooks('after_train')</code> inside its <code>train()</code> method. If it doesn't, hooks won't fire. The base class doesn't enforce this at runtime \u2014 it's a convention.</p>"},{"location":"architecture/base-trainer/#state-variables-that-hooks-read","title":"State Variables That Hooks Read","text":"<p>The base trainer maintains shared state that hooks (and other consumers) can inspect:</p> <pre><code>trainer.config          # The full merged config dictionary\ntrainer.model_name      # \"yolo\" or \"rfdetr\"\ntrainer.output_dir      # Path where weights and logs are saved\ntrainer.model           # The underlying model object\ntrainer.current_epoch   # Updated each epoch by the concrete trainer\ntrainer.current_loss    # Updated each epoch by the concrete trainer\n</code></pre> <p>This is the communication contract between trainers and hooks \u2014 trainers write to these fields, hooks read from them.</p>"},{"location":"architecture/base-trainer/#how-concrete-trainers-use-it","title":"How Concrete Trainers Use It","text":"YOLOTrainerRFDETRTrainer <pre><code>class YOLOTrainer(BaseTrainer):\n    def train(self):\n        self.build_model()\n        self._inject_hooks()       # Bridge Ultralytics \u2192 BaseTrainer hooks\n        self.call_hooks('before_train')\n        self.model.train(...)      # Ultralytics does the heavy lifting\n        self.call_hooks('after_train')\n</code></pre> <pre><code>class RFDETRTrainer(BaseTrainer):\n    def train(self):\n        self.build_model()\n        self.call_hooks('before_train')\n        self.model.train(...)      # Roboflow does the heavy lifting\n        self.call_hooks('after_train')\n        self._plot_loss_curves()\n</code></pre> <p>Both follow the same pattern: build \u2192 hook(before) \u2192 native train \u2192 hook(after), but their internals are completely different.</p>"},{"location":"architecture/overview/","title":"System Architecture","text":"<p>This page explains the overall design philosophy and how all the components fit together.</p>"},{"location":"architecture/overview/#design-philosophy","title":"Design Philosophy","text":"<p>IsiDetector follows three core principles:</p> <ol> <li>Config-Driven \u2014 Every tuneable parameter lives in YAML, never hardcoded</li> <li>Registry Pattern \u2014 Classes register themselves; the entrypoint discovers them at runtime</li> <li>Strategy Pattern \u2014 One abstract contract, multiple interchangeable implementations</li> </ol> <p>These patterns combine to create a system where adding a new model architecture requires zero changes to existing code \u2014 you just write a new trainer class and register it.</p>"},{"location":"architecture/overview/#full-architecture-diagram","title":"Full Architecture Diagram","text":"<pre><code>graph TB\n    subgraph \"\ud83c\udf9b\ufe0f Configuration Layer\"\n        YAML[\"train.yaml&lt;br/&gt;\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501&lt;br/&gt;model_type: rfdetr&lt;br/&gt;optimizer_config: ...\"]\n        OPT_Y[\"yolo_optim.yaml\"]\n        OPT_R[\"rfdetr_optim.yaml\"]\n    end\n\n    subgraph \"\ud83d\ude80 Orchestration Layer\"\n        RUN[\"run_train.py&lt;br/&gt;\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501&lt;br/&gt;1. Load + Merge YAMLs&lt;br/&gt;2. Registry Lookup&lt;br/&gt;3. Execute Pipeline\"]\n    end\n\n    subgraph \"\ud83e\udde9 Registry Layer\"\n        REG[\"Registry&lt;br/&gt;\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501&lt;br/&gt;TRAINERS: dict&lt;br/&gt;HOOKS: dict&lt;br/&gt;PREPROCESSORS: dict\"]\n    end\n\n    subgraph \"\ud83c\udfcb\ufe0f Training Layer\"\n        BASE[\"BaseTrainer (ABC)&lt;br/&gt;\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501&lt;br/&gt;build_model()&lt;br/&gt;train()&lt;br/&gt;evaluate()&lt;br/&gt;export()&lt;br/&gt;call_hooks()\"]\n        YOLO[\"YOLOTrainer&lt;br/&gt;\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501&lt;br/&gt;Ultralytics engine&lt;br/&gt;Auto data.yaml&lt;br/&gt;Callback bridging\"]\n        RFDETR[\"RFDETRTrainer&lt;br/&gt;\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501&lt;br/&gt;Roboflow engine&lt;br/&gt;DINOv2 backbone&lt;br/&gt;Loss curve plotting\"]\n    end\n\n    subgraph \"\ud83e\ude9d Hook Layer\"\n        HOOK[\"IndustrialLogger&lt;br/&gt;\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501&lt;br/&gt;before_train&lt;br/&gt;after_epoch&lt;br/&gt;after_train\"]\n        HOOK_N[\"Your Custom Hook&lt;br/&gt;\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501&lt;br/&gt;(just register it!)\"]\n    end\n\n    subgraph \"\ud83d\udd0d Inference Layer\"\n        BINF[\"BaseInferencer (ABC)\"]\n        YINF[\"YOLOInferencer\"]\n        RINF[\"RFDETRInferencer\"]\n    end\n\n    subgraph \"\ud83d\udee1\ufe0f Preprocessing Layer\"\n        PRE[\"SpecularGuard&lt;br/&gt;\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501&lt;br/&gt;CLAHE on L channel&lt;br/&gt;in LAB colour space\"]\n    end\n\n    YAML --&gt;|\"merge\"| OPT_Y\n    YAML --&gt;|\"merge\"| OPT_R\n    RUN --&gt;|\"1. load\"| YAML\n    RUN --&gt;|\"2. lookup\"| REG\n    REG --&gt;|\"returns\"| YOLO\n    REG --&gt;|\"returns\"| RFDETR\n    BASE --&gt;|\"inherits\"| YOLO\n    BASE --&gt;|\"inherits\"| RFDETR\n    YOLO --&gt;|\"broadcasts\"| HOOK\n    RFDETR --&gt;|\"broadcasts\"| HOOK\n    RFDETR -.-&gt;|\"broadcasts\"| HOOK_N\n    BINF --&gt;|\"inherits\"| YINF\n    BINF --&gt;|\"inherits\"| RINF\n    REG --&gt;|\"registers\"| HOOK\n    REG --&gt;|\"registers\"| PRE</code></pre>"},{"location":"architecture/overview/#the-five-layers","title":"The Five Layers","text":""},{"location":"architecture/overview/#1-configuration-layer","title":"1. Configuration Layer","text":"<p>Everything starts in YAML. The master config (<code>train.yaml</code>) holds global settings and points to model-specific optimizer configs that get merged at runtime.</p> <p>Config Merging</p> <p>The <code>optimizer_config</code> field in <code>train.yaml</code> points to a secondary YAML file. At startup, <code>run_train.py</code> reads both files and calls <code>config.update(optim_config)</code>, producing a single flat dictionary that gets passed to the trainer.</p> <p> Full Configuration Guide</p>"},{"location":"architecture/overview/#2-orchestration-layer","title":"2. Orchestration Layer","text":"<p><code>run_train.py</code> is the CLI entrypoint that:</p> <ol> <li>Parses <code>--config</code> and <code>--resume</code> arguments</li> <li>Loads and merges YAMLs into one config dict</li> <li>Reads <code>model_type</code> from config</li> <li>Calls <code>TRAINERS.get(model_type)</code> to get the right class</li> <li>Instantiates and runs the full pipeline: train \u2192 evaluate \u2192 export</li> </ol> <p>It has no awareness of what model it's running. It just trusts the registry.</p>"},{"location":"architecture/overview/#3-registry-layer","title":"3. Registry Layer","text":"<p>Three singleton registries act as name \u2192 class lookup tables. Classes register themselves with decorators at import time. The entrypoint triggers the imports, then looks up the right class by string name.</p> <p> Registry Deep-Dive</p>"},{"location":"architecture/overview/#4-training-layer","title":"4. Training Layer","text":"<p>The <code>BaseTrainer</code> abstract class defines the universal contract. Every trainer must implement four methods: <code>build_model()</code>, <code>train()</code>, <code>evaluate()</code>, and <code>export()</code>. The base also handles hook lifecycle management.</p> <p> BaseTrainer Deep-Dive</p>"},{"location":"architecture/overview/#5-support-layers","title":"5. Support Layers","text":"<p>Hooks listen to training lifecycle events. Inferencers run prediction. Preprocessors transform images before they enter the pipeline.</p> <p> Hooks \u00b7  Inference \u00b7  Preprocessing</p>"},{"location":"architecture/overview/#data-flow","title":"Data Flow","text":"<p>Here's exactly what happens when you run <code>python scripts/run_train.py</code>:</p> <pre><code>sequenceDiagram\n    participant User\n    participant CLI as run_train.py\n    participant Config as YAML Files\n    participant Reg as TRAINERS Registry\n    participant Trainer as Concrete Trainer\n    participant Hooks as Hook Chain\n\n    User-&gt;&gt;CLI: python scripts/run_train.py\n    CLI-&gt;&gt;Config: Load train.yaml\n    CLI-&gt;&gt;Config: Load + merge optimizer YAML\n    CLI-&gt;&gt;Reg: TRAINERS.get(\"rfdetr\")\n    Reg--&gt;&gt;CLI: RFDETRTrainer class\n\n    Note over CLI,Trainer: Instantiation Phase\n    CLI-&gt;&gt;Trainer: RFDETRTrainer(config)\n    Trainer-&gt;&gt;Hooks: Instantiate hooks from config['hooks']\n\n    Note over CLI,Hooks: Training Phase\n    CLI-&gt;&gt;Trainer: trainer.train()\n    Trainer-&gt;&gt;Hooks: call_hooks('before_train')\n    loop Every Epoch\n        Trainer-&gt;&gt;Trainer: Forward + backward pass\n        Trainer-&gt;&gt;Hooks: call_hooks('after_epoch')\n    end\n    Trainer-&gt;&gt;Hooks: call_hooks('after_train')\n\n    Note over CLI,Trainer: Post-Training\n    CLI-&gt;&gt;Trainer: trainer.evaluate()\n    CLI-&gt;&gt;Trainer: trainer.export('onnx')</code></pre>"},{"location":"architecture/registry/","title":"The Registry Pattern","text":"<p>The <code>Registry</code> class is the backbone of IsiDetector's plug-and-play architecture. It's a string \u2192 class lookup table that allows the system to discover and instantiate modules purely by name, with no hardcoded imports.</p>"},{"location":"architecture/registry/#why-use-a-registry","title":"Why Use a Registry?","text":"<p>Without a registry, you'd need code like this:</p> \u274c The Hardcoded Way<pre><code>if model_type == \"yolo\":\n    trainer = YOLOTrainer(config)\nelif model_type == \"rfdetr\":\n    trainer = RFDETRTrainer(config)\nelif model_type == \"new_model\":      # Every new model = edits here\n    trainer = NewModelTrainer(config)\n</code></pre> <p>With a registry:</p> \u2705 The Registry Way<pre><code>TrainerClass = TRAINERS.get(model_type)  # Works for ANY registered model\ntrainer = TrainerClass(config)\n</code></pre> <p>Open/Closed Principle</p> <p>The orchestration code is open for extension (new models) but closed for modification (no edits needed). This is a core SOLID principle.</p>"},{"location":"architecture/registry/#how-it-works","title":"How It Works","text":""},{"location":"architecture/registry/#the-registry-class","title":"The Registry Class","text":"<p> Source: <code>src/shared/registry.py</code></p> <pre><code>class Registry:\n    \"\"\"\n    A simple registry to map string names to classes.\n    Allows for decoupled, config-driven instantiation of modules.\n    \"\"\"\n    def __init__(self, name: str):\n        self._name = name           # (1)!\n        self._module_dict = dict()  # (2)!\n\n    def register(self, name: str = None):\n        \"\"\"Decorator to register a class with the registry.\"\"\"\n        def _register(cls):\n            module_name = name if name else cls.__name__  # (3)!\n            if module_name in self._module_dict:\n                logger.warning(f\"\u26a0\ufe0f Overwriting '{module_name}' in {self._name}!\")\n            self._module_dict[module_name] = cls          # (4)!\n            return cls\n        return _register\n\n    def get(self, name: str):\n        \"\"\"Retrieve a class from the registry by name.\"\"\"\n        if name not in self._module_dict:\n            available = list(self._module_dict.keys())\n            raise KeyError(f\"\u274c '{name}' not found. Available: {available}\")\n        return self._module_dict[name]                    # (5)!\n</code></pre> <ol> <li>Human-readable name for error messages (e.g., <code>\"Trainers\"</code>)</li> <li>The core storage \u2014 a plain Python dictionary mapping strings to classes</li> <li>If no name is explicitly given, falls back to the class's own <code>__name__</code></li> <li>Stores the class itself (not an instance) \u2014 instantiation happens later</li> <li>Returns the class, not an instance. The caller decides when to construct it</li> </ol>"},{"location":"architecture/registry/#the-three-global-registries","title":"The Three Global Registries","text":"<p>At the bottom of <code>registry.py</code>, three singleton instances are created:</p> <pre><code>PREPROCESSORS = Registry('Preprocessors')\nTRAINERS = Registry('Trainers')\nHOOKS = Registry('Hooks')\n</code></pre> Registry What it stores Example entries <code>TRAINERS</code> Model trainer classes <code>\"yolo\"</code> \u2192 <code>YOLOTrainer</code>, <code>\"rfdetr\"</code> \u2192 <code>RFDETRTrainer</code> <code>HOOKS</code> Training lifecycle hooks <code>\"IndustrialLogger\"</code> \u2192 <code>IndustrialLogger</code> <code>PREPROCESSORS</code> Image preprocessors <code>\"specular-guard\"</code> \u2192 <code>SpecularGuard</code>"},{"location":"architecture/registry/#registration-mechanism","title":"Registration Mechanism","text":"<p>Classes register themselves using the <code>@register()</code> decorator. This happens at import time \u2014 as soon as Python reads the file, the class gets added to the dictionary.</p>"},{"location":"architecture/registry/#single-registration","title":"Single Registration","text":"<pre><code>@TRAINERS.register('rfdetr')\nclass RFDETRTrainer(BaseTrainer):\n    ...\n</code></pre> <p>When Python imports this file, <code>TRAINERS._module_dict</code> becomes: <pre><code>{\"rfdetr\": RFDETRTrainer}\n</code></pre></p>"},{"location":"architecture/registry/#multiple-registration","title":"Multiple Registration","text":"<p>A class can register under multiple names:</p> <pre><code>@TRAINERS.register('yolov26')\n@TRAINERS.register('yolo')\nclass YOLOTrainer(BaseTrainer):\n    ...\n</code></pre> <p>Now both <code>TRAINERS.get('yolo')</code> and <code>TRAINERS.get('yolov26')</code> return <code>YOLOTrainer</code>.</p> <p>Decorator Order</p> <p>Decorators execute bottom-up. So <code>'yolo'</code> registers first, then <code>'yolov26'</code>. Both point to the same class object.</p>"},{"location":"architecture/registry/#the-import-trigger","title":"The Import Trigger","text":"<p>There's a critical detail: decorators only run when the module is imported. If nobody imports <code>yolo.py</code>, the <code>@TRAINERS.register('yolo')</code> decorator never fires.</p> <p>That's why <code>run_train.py</code> has these explicit \"wake-up\" imports:</p> scripts/run_train.py<pre><code>from src.shared.registry import TRAINERS\n\n# These imports look unused, but they trigger registration!\nimport src.training.trainers.yolo    # Registers 'yolo' and 'yolov26'\nimport src.training.trainers.rfdetr  # Registers 'rfdetr'\nimport src.training.hooks            # Registers 'IndustrialLogger'\n</code></pre> <p>Common Pitfall</p> <p>If you create a new trainer but forget to import its module in <code>run_train.py</code>, you'll get: <pre><code>\u274c 'my_new_model' not found in Trainers registry. Available: ['yolo', 'yolov26', 'rfdetr']\n</code></pre> Fix: add <code>import src.training.trainers.my_new_model</code> in <code>run_train.py</code>.</p>"},{"location":"architecture/registry/#adding-a-new-module","title":"Adding a New Module","text":"<p>Here's the complete checklist for adding, say, a YOLO-NAS trainer:</p> Step 1: Create the TrainerStep 2: Wake It UpStep 3: Use It src/training/trainers/yolo_nas.py<pre><code>from src.training.base_trainer import BaseTrainer\nfrom src.shared.registry import TRAINERS\n\n@TRAINERS.register('yolo-nas')\nclass YOLONASTrainer(BaseTrainer):\n    def build_model(self):\n        ...\n    def train(self):\n        ...\n    def evaluate(self):\n        ...\n    def export(self, format='onnx'):\n        ...\n</code></pre> scripts/run_train.py<pre><code>import src.training.trainers.yolo_nas  # Add this line\n</code></pre> configs/train.yaml<pre><code>model_type: \"yolo-nas\"\n</code></pre> <p>No other files need to change. The registry, base trainer, hooks, and <code>run_train.py</code> pipeline all work automatically.</p>"},{"location":"architecture/registry/#internal-state-after-all-imports","title":"Internal State After All Imports","text":"<p>After <code>run_train.py</code> runs all its imports, the registries look like this:</p> <pre><code>graph LR\n    subgraph \"TRAINERS Registry\"\n        A[\"'yolo'\"] --&gt; YT[\"YOLOTrainer\"]\n        B[\"'yolov26'\"] --&gt; YT\n        C[\"'rfdetr'\"] --&gt; RT[\"RFDETRTrainer\"]\n    end\n\n    subgraph \"HOOKS Registry\"\n        D[\"'IndustrialLogger'\"] --&gt; IL[\"IndustrialLogger\"]\n    end\n\n    subgraph \"PREPROCESSORS Registry\"\n        E[\"'specular-guard'\"] --&gt; SG[\"SpecularGuard\"]\n    end</code></pre>"},{"location":"config/","title":"Configuration System","text":"<p>IsiDetector uses a two-tier YAML configuration system. A master config (<code>train.yaml</code>) defines what model to use and points to a model-specific optimizer config that gets merged at runtime.</p>"},{"location":"config/#config-architecture","title":"Config Architecture","text":"<pre><code>graph TB\n    subgraph \"Tier 1: Master Config\"\n        A[\"train.yaml&lt;br/&gt;\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501&lt;br/&gt;model_type&lt;br/&gt;dataset_path&lt;br/&gt;hooks&lt;br/&gt;class_names&lt;br/&gt;optimizer_config \u2192\"]\n    end\n\n    subgraph \"Tier 2: Optimizer Configs\"\n        B[\"yolo_optim.yaml&lt;br/&gt;\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501&lt;br/&gt;epochs&lt;br/&gt;optimizer{}&lt;br/&gt;scheduler{}&lt;br/&gt;early_stopping{}&lt;br/&gt;checkpoint{}\"]\n        C[\"rfdetr_optim.yaml&lt;br/&gt;\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501\u2501&lt;br/&gt;epochs&lt;br/&gt;optimizer{}&lt;br/&gt;training_tricks{}&lt;br/&gt;early_stopping{}\"]\n    end\n\n    A --&gt;|\"merge\"| B\n    A --&gt;|\"merge\"| C</code></pre>"},{"location":"config/#how-merging-works","title":"How Merging Works","text":"<p>In <code>run_train.py</code>, the two configs are combined with a simple <code>dict.update()</code>:</p> scripts/run_train.py<pre><code># Load master config\nwith open(config_path, 'r') as f:\n    config = yaml.safe_load(f)\n\n# Merge optimizer config\nif 'optimizer_config' in config:\n    optim_path = PROJECT_ROOT / config['optimizer_config']\n    with open(optim_path, 'r') as f:\n        optim_config = yaml.safe_load(f)\n        config.update(optim_config)                     # (1)!\n</code></pre> <ol> <li><code>dict.update()</code> merges the optimizer config into the main config. If both have the same key (e.g., <code>batch_size</code>), the optimizer config wins because it's applied second.</li> </ol> <p>Override Order</p> <p>Keys in the optimizer YAML will override keys in <code>train.yaml</code>. For example, if both files define <code>batch_size</code>, the optimizer config's value takes effect.</p>"},{"location":"config/#example-merged-result","title":"Example Merged Result","text":"train.yamlrfdetr_optim.yamlMerged Result <pre><code>project_name: \"isiDetector\"\nnc: 2\nclass_names: [\"carton\", \"polybag\"]\nhooks:\n  - \"IndustrialLogger\"\nmodel_type: \"rfdetr\"\nmodel_size: \"m\"\noptimizer_config: \"configs/optimizers/rfdetr_optim.yaml\"\ndataset_path: \"data/rfdetr_dataset\"\nbatch_size: 4\nimage_size: 640\nmixed_precision: true\n</code></pre> <pre><code>epochs: 101\nbatch_size: 4\nimage_size: 640\noptimizer:\n  type: \"AdamW\"\n  lr: 0.0001\n  lr_encoder: 0.00001\n  weight_decay: 0.0001\ntraining_tricks:\n  use_ema: true\n  grad_accum_steps: 4\nearly_stopping:\n  enabled: true\n  patience: 7\n</code></pre> <pre><code># From train.yaml:\nproject_name: \"isiDetector\"\nnc: 2\nclass_names: [\"carton\", \"polybag\"]\nhooks: [\"IndustrialLogger\"]\nmodel_type: \"rfdetr\"\nmodel_size: \"m\"\noptimizer_config: \"configs/optimizers/rfdetr_optim.yaml\"\ndataset_path: \"data/rfdetr_dataset\"\nmixed_precision: true\n\n# From rfdetr_optim.yaml (merged):\nepochs: 101\nbatch_size: 4\nimage_size: 640\noptimizer:\n  type: \"AdamW\"\n  lr: 0.0001\n  lr_encoder: 0.00001\n  weight_decay: 0.0001\ntraining_tricks:\n  use_ema: true\n  grad_accum_steps: 4\nearly_stopping:\n  enabled: true\n  patience: 7\n</code></pre>"},{"location":"config/#the-engine-switchboard","title":"The Engine Switchboard","text":"<p>The master <code>train.yaml</code> acts as a switchboard. Only one engine block should be uncommented at a time:</p> configs/train.yaml<pre><code># =========================================================\n# \ud83d\uded1 ACTIVE ENGINE SWITCHBOARD (Uncomment ONE block below)\n# =========================================================\n\n# ---------------------------------------------------------\n# \ud83d\ude80 OPTION A: YOLOv8 (CNN) - Fast, grid-based\n# ---------------------------------------------------------\n# model_type: \"yolo\"\n# model_size: \"m\"\n# optimizer_config: \"configs/optimizers/yolo_optim.yaml\"\n# dataset_path: \"data/isi_3k_dataset\"\n# batch_size: 16\n# image_size: 640\n\n# ---------------------------------------------------------\n# \ud83e\udd16 OPTION B: RF-DETR (Transformer) - DINOv2 Backbone\n# ---------------------------------------------------------\nmodel_type: \"rfdetr\"\nmodel_size: \"m\"\noptimizer_config: \"configs/optimizers/rfdetr_optim.yaml\"\ndataset_path: \"data/rfdetr_dataset\"\nbatch_size: 4\nimage_size: 640\n</code></pre>"},{"location":"config/#complete-config-reference","title":"Complete Config Reference","text":""},{"location":"config/#global-keys-trainyaml","title":"Global Keys (train.yaml)","text":"Key Type Required Description <code>project_name</code> string No Project identifier for logging <code>model_type</code> string Yes Registry key: <code>\"yolo\"</code>, <code>\"yolov26\"</code>, or <code>\"rfdetr\"</code> <code>model_size</code> string No <code>\"n\"</code>, <code>\"s\"</code>, <code>\"m\"</code>, <code>\"l\"</code>, <code>\"x\"</code> <code>optimizer_config</code> string No Path to secondary YAML (relative to project root) <code>dataset_path</code> string Yes Path to dataset directory <code>nc</code> int No Number of classes (default: 2) <code>class_names</code> list No Class label list <code>hooks</code> list No Hook names to attach <code>batch_size</code> int No Training batch size <code>image_size</code> int No Input resolution <code>mixed_precision</code> bool No Enable AMP (default: true)"},{"location":"config/#yolo-specific-keys","title":"YOLO-Specific Keys","text":"Key Type Default Description <code>hsv_h</code> float 0.015 Hue augmentation range <code>hsv_s</code> float 0.7 Saturation augmentation range <code>hsv_v</code> float 0.4 Value augmentation range <code>fliplr</code> float 0.5 Horizontal flip probability <code>flipud</code> float 0.0 Vertical flip probability <code>mosaic</code> float 1.0 Mosaic augmentation probability <code>scale</code> float 0.5 Scale augmentation range <code>translate</code> float 0.1 Translation augmentation range <code>degrees</code> float 0.0 Rotation augmentation range"},{"location":"config/#optimizer-keys","title":"Optimizer Keys","text":"Key Type Used By Description <code>optimizer.type</code> string Both Optimizer name (e.g., <code>\"AdamW\"</code>) <code>optimizer.lr</code> float Both Base learning rate <code>optimizer.lr_encoder</code> float RF-DETR only Backbone learning rate <code>optimizer.weight_decay</code> float Both Weight decay <code>optimizer.scheduler.type</code> string YOLO only <code>\"CosineAnnealing\"</code> <code>optimizer.scheduler.warmup_epochs</code> float YOLO only Warmup length <code>optimizer.scheduler.eta_min</code> float YOLO only Minimum LR"},{"location":"config/#early-stopping-keys","title":"Early Stopping Keys","text":"Key Type Default Description <code>early_stopping.enabled</code> bool true Enable early stopping <code>early_stopping.patience</code> int 10/7 Epochs without improvement before stop <code>early_stopping.min_delta</code> float 0.0005 Minimum improvement threshold <code>early_stopping.monitor</code> string <code>\"mAP50\"</code> Metric to monitor"},{"location":"config/#training-tricks-rf-detr-only","title":"Training Tricks (RF-DETR only)","text":"Key Type Default Description <code>training_tricks.use_ema</code> bool true Exponential Moving Average <code>training_tricks.grad_accum_steps</code> int 4 Gradient accumulation steps"},{"location":"config/#cli-arguments","title":"CLI Arguments","text":"<pre><code>python scripts/run_train.py [OPTIONS]\n</code></pre> Argument Default Description <code>--config</code> <code>configs/train.yaml</code> Path to master config <code>--resume</code> <code>None</code> Path to <code>last.pt</code> checkpoint (YOLO resume only)"},{"location":"config/#examples","title":"Examples","text":"<pre><code># Default training\npython scripts/run_train.py\n\n# Custom config\npython scripts/run_train.py --config configs/my_custom.yaml\n\n# Resume from checkpoint\npython scripts/run_train.py --resume models/yolo/24-03-2026/weights/last.pt\n</code></pre>"},{"location":"hooks/","title":"Hook System","text":"<p>The hook system provides a lightweight Observer pattern for reacting to training lifecycle events without modifying trainer code. Hooks are registered by name and attached via YAML config.</p>"},{"location":"hooks/#how-hooks-work","title":"How Hooks Work","text":"<pre><code>graph LR\n    subgraph \"train.yaml\"\n        A[\"hooks:&lt;br/&gt;  - IndustrialLogger&lt;br/&gt;  - YourCustomHook\"]\n    end\n    subgraph \"BaseTrainer.__init__()\"\n        B[\"HOOKS.get('IndustrialLogger')\"]\n        C[\"Instantiate + append\"]\n    end\n    subgraph \"Training Loop\"\n        D[\"call_hooks('before_train')\"]\n        E[\"call_hooks('after_epoch')\"]\n        F[\"call_hooks('after_train')\"]\n    end\n\n    A --&gt; B --&gt; C --&gt; D --&gt; E --&gt; F</code></pre>"},{"location":"hooks/#lifecycle-stages","title":"Lifecycle Stages","text":"Stage When It Fires Common Use <code>before_train</code> Before first epoch Print headers, start timers, init connections <code>before_epoch</code> Before each epoch Reset per-epoch counters <code>after_epoch</code> After each epoch Log metrics, save checkpoints, send alerts <code>after_train</code> After final epoch Print summary, close connections, cleanup"},{"location":"hooks/#what-hooks-receive","title":"What Hooks Receive","text":"<p>Every hook method receives the trainer instance as its argument:</p> <pre><code>def after_epoch(self, trainer):\n    trainer.current_epoch    # Current epoch number\n    trainer.current_loss     # Current training loss\n    trainer.config           # Full merged config dict\n    trainer.output_dir       # Path to output directory\n    trainer.model_name       # \"yolo\" or \"rfdetr\"\n</code></pre>"},{"location":"hooks/#industriallogger-the-built-in-hook","title":"IndustrialLogger \u2014 The Built-In Hook","text":"<p> Source: <code>src/training/hooks/industrial_logger.py</code> Registry Name: <code>\"IndustrialLogger\"</code></p> <p>A formatted, table-style epoch logger designed for industrial CV workflows:</p> <pre><code>@HOOKS.register('IndustrialLogger')\nclass IndustrialLogger:\n    \"\"\"A clean, epoch-level logger for Industrial Computer Vision.\"\"\"\n\n    def before_train(self, trainer):                        # (1)!\n        header = (\n            f\"\\n{'Epoch':&gt;8} {'GPU_mem':&gt;10} {'box_loss':&gt;10} \"\n            f\"{'seg_loss':&gt;10} {'cls_loss':&gt;10} {'dfl_loss':&gt;10} \"\n            f\"{'Instances':&gt;10} {'Size':&gt;8}\"\n        )\n        print(header)\n        print(\"-\" * len(header))\n\n    def after_epoch(self, trainer):                         # (2)!\n        epoch_str = f\"{trainer.current_epoch + 1}/{trainer.config.get('epochs', 30)}\"\n        gpu_mem = f\"{torch.cuda.memory_reserved(0) / 1e9:.2f}G\"\n\n        row = (\n            f\"{epoch_str:&gt;8} {gpu_mem:&gt;10} \"\n            f\"{trainer.current_loss:&gt;10.4f} {'--':&gt;10} \"\n            f\"{'--':&gt;10} {'--':&gt;10} {'--':&gt;10} \"\n            f\"{trainer.config.get('image_size', 640):&gt;8}\"\n        )\n        print(row)\n\n    def after_train(self, trainer):                         # (3)!\n        print(\"-\" * 80)\n        logger.info(f\"\u2705 Training Complete. Weights at {trainer.output_dir}\")\n</code></pre> <ol> <li>Prints a formatted table header at the start of training</li> <li>Prints one row per epoch with epoch progress, GPU memory, and loss</li> <li>Prints a completion message with the output directory</li> </ol> <p>Sample output:</p> <pre><code>   Epoch    GPU_mem   box_loss   seg_loss   cls_loss   dfl_loss  Instances     Size\n---------------------------------------------------------------------------------------\n   1/200      4.23G     1.2345         --         --         --         --      640\n   2/200      4.23G     1.1892         --         --         --         --      640\n   3/200      4.24G     1.0456         --         --         --         --      640\n</code></pre>"},{"location":"hooks/#creating-a-custom-hook","title":"Creating a Custom Hook","text":"<p>Adding a new hook requires three steps:</p>"},{"location":"hooks/#step-1-write-the-hook-class","title":"Step 1: Write the Hook Class","text":"src/training/hooks/slack_alert.py<pre><code>import logging\nfrom src.shared.registry import HOOKS\n\nlogger = logging.getLogger(__name__)\n\n@HOOKS.register('SlackAlert')\nclass SlackAlert:\n    \"\"\"Sends a Slack message when training finishes.\"\"\"\n\n    def __init__(self):\n        self.webhook_url = \"https://hooks.slack.com/services/YOUR/WEBHOOK\"\n\n    def after_train(self, trainer):\n        import requests\n        message = (\n            f\"\ud83c\udfc1 Training Complete!\\n\"\n            f\"Model: {trainer.model_name}\\n\"\n            f\"Final Loss: {trainer.current_loss:.4f}\\n\"\n            f\"Weights: {trainer.output_dir}\"\n        )\n        requests.post(self.webhook_url, json={\"text\": message})\n        logger.info(\"\ud83d\udce8 Slack notification sent!\")\n</code></pre>"},{"location":"hooks/#step-2-import-it","title":"Step 2: Import It","text":"scripts/run_train.py<pre><code>import src.training.hooks.slack_alert  # Add this line\n</code></pre>"},{"location":"hooks/#step-3-enable-it-in-config","title":"Step 3: Enable It in Config","text":"configs/train.yaml<pre><code>hooks:\n  - \"IndustrialLogger\"\n  - \"SlackAlert\"            # Add this line\n</code></pre> <p>That's it. The hook system handles the rest.</p> <p>Hooks Are Optional Per-Stage</p> <p>A hook doesn't need to implement every stage. If <code>SlackAlert</code> only has <code>after_train</code>, it's simply skipped during <code>before_train</code> and <code>after_epoch</code> broadcasts.</p>"},{"location":"inference/","title":"Inference Engines","text":"<p>The inference layer mirrors the training layer's design \u2014 an abstract <code>BaseInferencer</code> defines the contract, and concrete implementations handle YOLO and RF-DETR specifics.</p>"},{"location":"inference/#architecture","title":"Architecture","text":"<pre><code>classDiagram\n    class BaseInferencer {\n        &lt;&lt;abstract&gt;&gt;\n        +model_path: Path\n        +conf_threshold: float\n        +model: Any\n        +_load_model()*\n        +predict(source, show, save)*\n        +get_summary(result)* dict\n    }\n\n    class YOLOInferencer {\n        +class_names: dict\n        +predict_frame(frame) Detections\n        +predict(source) Generator\n        +get_summary(result) dict\n    }\n\n    class RFDETRInferencer {\n        +class_names: dict\n        +predict_frame(frame) Detections\n        +predict(source) Generator\n        +get_summary(result) dict\n    }\n\n    BaseInferencer &lt;|-- YOLOInferencer\n    BaseInferencer &lt;|-- RFDETRInferencer</code></pre>"},{"location":"inference/#baseinferencer","title":"BaseInferencer","text":"<p> Source: <code>src/inference/base_inferencer.py</code></p> <pre><code>class BaseInferencer(ABC):\n    def __init__(self, model_path: str, conf_threshold: float = 0.5):\n        self.model_path = Path(model_path)\n        self.conf_threshold = conf_threshold\n\n        if not self.model_path.exists():\n            raise FileNotFoundError(f\"\u274c Weights not found at {self.model_path}\")\n\n        self.model = self._load_model()\n</code></pre> Abstract Method Returns Purpose <code>_load_model()</code> Model object Load weights into RAM/VRAM <code>predict(source)</code> Generator of dicts Run inference (yields results one by one) <code>get_summary(result)</code> <code>dict</code> Translate raw output into simple metrics"},{"location":"inference/#yoloinferencer","title":"YOLOInferencer","text":"<p> Source: <code>src/inference/yolo_inferencer.py</code></p>"},{"location":"inference/#frame-level-prediction","title":"Frame-Level Prediction","text":"<p>The <code>predict_frame()</code> method handles single video frames with intelligent 640px scaling:</p> <pre><code>def predict_frame(self, frame: np.ndarray):\n    orig_h, orig_w = frame.shape[:2]\n\n    # Only downsize if larger than 640px\n    if max(orig_h, orig_w) &gt; 640:\n        scale = 640 / max(orig_h, orig_w)\n        new_w, new_h = int(orig_w * scale), int(orig_h * scale)\n        input_frame = cv2.resize(frame, (new_w, new_h))        # (1)!\n    else:\n        input_frame = frame\n        scale = 1.0\n\n    # Run inference at 640px\n    results = self.model(input_frame, conf=self.conf_threshold,\n                         imgsz=640, verbose=False)[0]\n    detections = sv.Detections.from_ultralytics(results)        # (2)!\n\n    # Scale detections back to original resolution\n    if needs_scaling and len(detections) &gt; 0:\n        inv_scale = 1 / scale\n        detections.xyxy *= inv_scale                            # (3)!\n\n        if detections.mask is not None:\n            resized_masks = []\n            for mask in detections.mask:\n                m = cv2.resize(mask.astype(\"uint8\"),\n                             (orig_w, orig_h),\n                             interpolation=cv2.INTER_NEAREST)   # (4)!\n                resized_masks.append(m.astype(bool))\n            detections.mask = np.array(resized_masks)\n\n    return detections\n</code></pre> <ol> <li>Downscale for faster inference while maintaining aspect ratio</li> <li>Convert from Ultralytics' custom format to <code>supervision.Detections</code> \u2014 a standardised format</li> <li>Scale bounding boxes back to the original image resolution</li> <li>Segmentation masks are resized with nearest-neighbour interpolation to avoid blurry mask edges</li> </ol>"},{"location":"inference/#stream-level-prediction","title":"Stream-Level Prediction","text":"<pre><code>def predict(self, source: str, show=False, save=False):\n    source_val = int(source) if str(source).isdigit() else source  # (1)!\n    results_gen = self.model.predict(\n        source=source_val, conf=self.conf_threshold,\n        stream=True, verbose=False\n    )\n    for r in results_gen:\n        detections = sv.Detections.from_ultralytics(r)\n        yield {\"path\": r.path, \"detections\": detections, \"raw\": r}\n</code></pre> <ol> <li>If source is <code>\"0\"</code>, converts to <code>int(0)</code> for webcam access. Otherwise treats as file path / URL.</li> </ol>"},{"location":"inference/#rfdetrinferencer","title":"RFDETRInferencer","text":"<p> Source: <code>src/inference/rfdetr_inferencer.py</code></p>"},{"location":"inference/#key-differences-from-yolo","title":"Key Differences from YOLO","text":"Aspect YOLO RF-DETR Input format NumPy array (BGR) PIL Image (RGB) Model loading <code>YOLO(path)</code> <code>RFDETRSegMedium(pretrain_weights=path)</code> Optimization N/A <code>model.optimize_for_inference()</code> Prediction API <code>model(frame)</code> <code>model.predict(image, threshold=...)</code> <pre><code>def _load_model(self):\n    model = RFDETRSegMedium(pretrain_weights=str(self.model_path))\n    model.optimize_for_inference()                          # (1)!\n    return model\n</code></pre> <ol> <li>RF-DETR-specific optimization that fuses layers and applies TorchScript tracing for faster inference</li> </ol>"},{"location":"inference/#built-in-visualisation","title":"Built-In Visualisation","text":"<p>The RF-DETR inferencer includes a full visualisation pipeline using <code>supervision</code>:</p> <pre><code>def _visualize(self, path, detections, show, save):\n    cv_image = cv2.imread(str(path))\n\n    # Three-layer annotation: masks \u2192 boxes \u2192 labels\n    annotated = sv.MaskAnnotator().annotate(scene=cv_image,\n                                            detections=detections)\n    annotated = sv.BoxAnnotator().annotate(scene=annotated,\n                                           detections=detections)\n    labels = [f\"{self.class_names.get(c_id, 'obj')} {conf:.2f}\"\n              for c_id, conf in zip(detections.class_id,\n                                     detections.confidence)]\n    annotated = sv.LabelAnnotator().annotate(scene=annotated,\n                                             detections=detections,\n                                             labels=labels)\n</code></pre>"},{"location":"inference/#using-the-inferencers","title":"Using the Inferencers","text":"YOLO \u2014 Single ImageYOLO \u2014 Live CameraRF-DETR \u2014 Single Image <pre><code>from src.inference.yolo_inferencer import YOLOInferencer\n\nengine = YOLOInferencer(\"models/yolo/best.pt\", conf_threshold=0.5)\nfor result in engine.predict(\"data/test1.jpg\"):\n    print(engine.get_summary(result))\n</code></pre> <pre><code>engine = YOLOInferencer(\"models/yolo/best.pt\")\ncap = cv2.VideoCapture(0)\nwhile True:\n    ret, frame = cap.read()\n    detections = engine.predict_frame(frame)\n    # Draw and display...\n</code></pre> <pre><code>from src.inference.rfdetr_inferencer import RFDETRInferencer\n\nengine = RFDETRInferencer(\"models/rfdetr/best.pt\", conf_threshold=0.5)\nfor result in engine.predict(\"data/test1.jpg\", show=True):\n    print(engine.get_summary(result))\n</code></pre>"},{"location":"inference/onnx/","title":"High-Speed ONNX Engine","text":"<p>The <code>ONNXInferencer</code> is specifically designed for high-throughput industrial environments. It bypasses the overhead of heavy deep learning frameworks like PyTorch or Ultralytics during the inference phase, using ONNX Runtime for maximum CPU/GPU efficiency.</p>"},{"location":"inference/onnx/#technical-overview","title":"Technical Overview","text":"<p> Source: <code>src/inference/onnx_inferencer.py</code></p>"},{"location":"inference/onnx/#1-vectorized-mask-decoding","title":"1. Vectorized Mask Decoding","text":"<p>A major bottleneck in segmentation is decoding the model's Proto-masks into human-readable masks. While standard implementations use slow Python loops, our <code>ONNXInferencer</code> uses NumPy Broadcasting (Vectorization) to process all masks simultaneously.</p> <p>Instead of: <pre><code># Slow way (Python Loop)\nfor i in range(n):\n    mask = sigmoid(matrix_mult(protos, coeffs[i]))\n</code></pre></p> <p>We use: <pre><code># Fast way (Vectorized)\n# Compute all masks in a single matrix operation\nmasks = sigmoid(protos @ coeffs.T)\n</code></pre></p>"},{"location":"inference/onnx/#2-dynamic-input-scaling","title":"2. Dynamic Input Scaling","text":"<p>Unlike hardcoded scripts, this engine dynamically extracts the required input shape directly from the <code>.onnx</code> file metadata. This ensures compatibility with any model resolution (e.g., 320x320, 640x640, or 1280x1280) without code changes.</p>"},{"location":"inference/onnx/#3-automatic-class-mapping","title":"3. Automatic Class Mapping","text":"<p>The engine automatically handles the differences between YOLO and RF-DETR output structures: - YOLO: Dynamically slices the score tensor based on the class count in your <code>train.yaml</code>. - RF-DETR: Automatically shifts class indices by +1 to align with Transformer-based COCO conventions.</p>"},{"location":"inference/onnx/#when-to-use","title":"When to Use?","text":"<ul> <li>Live Streams: When processing 30 FPS RTSP feeds.</li> <li>Edge Devices: When running on hardware with limited CPU power.</li> <li>Production: This is our most stable and performant engine for long-term deployment.</li> </ul>"},{"location":"modules/","title":"Modules","text":"<p>Supporting components that plug into the training and inference pipeline.</p>"},{"location":"modules/#hook-system","title":"\ud83e\ude9d Hook System","text":"<p>Attach lifecycle listeners to training. Built-in <code>IndustrialLogger</code> + guide for creating custom hooks.</p> <p> Read More</p>"},{"location":"modules/#inference-engines","title":"\ud83d\udd0d Inference Engines","text":"<p>Model-agnostic prediction with 640px scaling, frame-level and stream-level APIs. Supports YOLO and RF-DETR.</p> <p> Read More</p>"},{"location":"modules/#preprocessing","title":"\ud83d\udee1\ufe0f Preprocessing","text":"<p>SpecularGuard \u2014 CLAHE in LAB colour space for industrial lighting. Tames polybag glare and lifts conveyor shadows.</p> <p> Read More</p>"},{"location":"modules/analytics/","title":"Industrial Analytics &amp; Logging","text":"<p>The <code>DailyLogger</code> provides a robust telemetry layer for industrial parcel sorting. It ensures that every detection is recorded accurately for long-term auditing and business intelligence.</p>"},{"location":"modules/analytics/#the-dailylogger","title":"The DailyLogger","text":"<p> Source: <code>src/utils/analytics_logger.py</code></p>"},{"location":"modules/analytics/#how-it-works","title":"How it Works","text":"<p>The logger is designed to be \"zero-maintenance.\" It automatically handles:</p> <ol> <li>Daily Rotation: Every day at midnight, it creates a new CSV file (e.g., <code>report_27-03-2026.csv</code>).</li> <li>Hourly Snapshots: Instead of writing to disk every millisecond (which kills SSD life), it takes a snapshot of the totals according to your <code>save_interval</code> (Default: 1 Hour).</li> <li>Graceful Persistence: Even if the system crashes or is stopped manually, the logger captures the final state before closing.</li> </ol>"},{"location":"modules/analytics/#csv-structure","title":"CSV Structure","text":"<p>The generated reports are standard CSV files that can be opened in Excel, Google Sheets, or any BI tool (PowerBI/Tableau).</p> Time CARTON POLYBAG 09:00:00 45 12 10:00:00 112 34 11:00:00 258 89"},{"location":"modules/analytics/#configuration","title":"Configuration","text":"<p>You can control the behavior of the analytics layer via <code>configs/train.yaml</code>:</p> <pre><code>inference:\n  logging:\n    enabled: true\n    save_interval: 3600    # Time in seconds (3600 = 1 Hour)\n    log_dir: \"logs\"        # Destination folder\n</code></pre>"},{"location":"modules/vision-engine/","title":"Unified Vision Engine","text":"<p>The <code>VisionEngine</code> is the \"brain\" of the IsiDetector platform. It unifies the detection logic so that your Command Line scripts and your Web Application behave identically.</p>"},{"location":"modules/vision-engine/#architecture","title":"Architecture","text":"<p> Source: <code>src/shared/vision_engine.py</code></p> <p>The engine encapsulates the entire computer vision pipeline into a single, stateful class:</p> <pre><code>graph TD\n    Frame[Raw Video Frame] --&gt; Engine[VisionEngine]\n    Engine --&gt; Inf[Inferencer]\n    Inf --&gt; Track[ByteTrack]\n    Track --&gt; Line[LineZone Counter]\n    Line --&gt; Log[DailyLogger]\n    Log --&gt; Output[Annotated Frame + Events]</code></pre>"},{"location":"modules/vision-engine/#key-functionalities","title":"Key Functionalities","text":""},{"location":"modules/vision-engine/#1-tracking-continuity","title":"1. Tracking Continuity","text":"<p>By wrapping the <code>sv.ByteTrack</code> module, the engine assigns persistent IDs to every parcel (e.g., <code>#104</code>). This ensures that even if a parcel is briefly obscured, the system remembers it until it crosses the counting line.</p>"},{"location":"modules/vision-engine/#2-trigger-management","title":"2. Trigger Management","text":"<p>The <code>process_frame()</code> method handles the precise logic of \"Line Crossing\".  - It detects when an object's Bottom Center point crosses the designated vertical line. - It ensures an object is only counted once per session, even if it hovers over the line.</p>"},{"location":"modules/vision-engine/#3-real-time-visualization","title":"3. Real-Time Visualization","text":"<p>The engine generates a fully annotated frame including: - Segmentation Masks: Transparent color overlays on the parcels. - Tracing: Visual lines showing the path the parcel has taken. - Labels: High-contrast text overlays with Object IDs and Class Names.</p>"},{"location":"modules/vision-engine/#implementation-example","title":"Implementation Example","text":"<pre><code>from src.shared.vision_engine import VisionEngine\nfrom src.inference.onnx_inferencer import ONNXInferencer\n\n# 1. Initialize\nengine = VisionEngine(\n    inferencer=ONNXInferencer(\"best.onnx\"),\n    config=my_config_dict\n)\n\n# 2. Process\nannotated, detections, event = engine.process_frame(frame, counts)\n\n# 3. Handle Events\nif event:\n    print(f\"Parcel {event['id']} ({event['class']}) crossed the line!\")\n</code></pre>"},{"location":"preprocessing/","title":"Preprocessing \u2014 SpecularGuard","text":"<p>The <code>SpecularGuard</code> preprocessor solves a common industrial imaging problem: polybag glare and deep conveyor shadows that confuse segmentation models.</p> <p> Source: <code>src/preprocess/clahe_engine.py</code> Registry Name: <code>\"specular-guard\"</code></p>"},{"location":"preprocessing/#the-problem","title":"The Problem","text":"<p>In warehouse environments, two lighting issues dominate:</p> <ol> <li>Specular reflections on polybags \u2014 shiny plastic creates white hotspots that wash out texture</li> <li>Deep shadows under stacked cartons \u2014 dark regions lose detail and get missed by detectors</li> </ol> <p>Standard approaches like histogram equalisation affect the entire image including colour channels, which distorts the actual parcel appearance. This leads to false positives and poor segmentation boundaries.</p>"},{"location":"preprocessing/#the-solution-clahe-in-lab-colour-space","title":"The Solution: CLAHE in LAB Colour Space","text":"<p>SpecularGuard uses Contrast Limited Adaptive Histogram Equalisation (CLAHE) applied only to the Lightness channel of the LAB colour space.</p>"},{"location":"preprocessing/#why-lab","title":"Why LAB?","text":"Colour Space Problem RGB Channels are correlated. Equalising one changes the perceived colour HSV V (Value) affects saturation perception. Equalising V shifts colours LAB L (Lightness) is completely independent of colour (a, b). Equalising L only changes brightness, not colour <pre><code>graph LR\n    A[\"BGR Input\"] --&gt;|\"cvtColor\"| B[\"LAB\"]\n    B --&gt; C[\"L channel\"]\n    B --&gt; D[\"a channel (untouched)\"]\n    B --&gt; E[\"b channel (untouched)\"]\n    C --&gt;|\"CLAHE\"| F[\"L enhanced\"]\n    F --&gt; G[\"Merge L+a+b\"]\n    D --&gt; G\n    E --&gt; G\n    G --&gt;|\"cvtColor\"| H[\"BGR Output\"]</code></pre>"},{"location":"preprocessing/#how-clahe-works","title":"How CLAHE Works","text":"<p>Standard histogram equalisation spreads pixel intensities across the full range (0\u2013255). But it has two problems:</p> <ol> <li>It's global \u2014 doesn't handle local variations well</li> <li>It can over-amplify noise in dark regions</li> </ol> <p>CLAHE fixes both:</p> <ul> <li>Adaptive: Divides the image into tiles (e.g., 8\u00d78) and equalises each tile independently, handling both bright and dark regions</li> <li>Contrast Limited: Clips the histogram at a threshold (<code>clipLimit</code>) before equalising, preventing noise amplification</li> </ul>"},{"location":"preprocessing/#annotated-source","title":"Annotated Source","text":"<pre><code>@PREPROCESSORS.register('specular-guard')\nclass SpecularGuard:\n    \"\"\"\n    Enhances image lightness in LAB colour space to preserve\n    actual parcel colours while lifting shadows and stabilising glare.\n    \"\"\"\n    def __init__(self, clip_limit: float = 2.5,\n                 tile_grid: list = [8, 8]):\n        grid_tuple = tuple(tile_grid)\n        self.clahe = cv2.createCLAHE(\n            clipLimit=clip_limit,       # (1)!\n            tileGridSize=grid_tuple     # (2)!\n        )\n</code></pre> <ol> <li><code>clipLimit=2.5</code>: Controls how much contrast enhancement is allowed. Higher = more enhancement but more noise. 2.5 is a balanced default for warehouse lighting</li> <li><code>tileGridSize=(8, 8)</code>: Divides the image into an 8\u00d78 grid of tiles. Each tile gets independent equalisation. Smaller tiles = more local adaptation but slower</li> </ol> <pre><code>    def process(self, image: np.ndarray) -&gt; np.ndarray:\n        if image is None:\n            raise ValueError(\"Received empty image array.\")\n\n        # 1. Convert to LAB\n        lab = cv2.cvtColor(image, cv2.COLOR_BGR2LAB)\n        l, a, b = cv2.split(lab)\n\n        # 2. Apply CLAHE to L channel ONLY\n        l_enhanced = self.clahe.apply(l)        # (1)!\n\n        # 3. Reconstruct and return\n        enhanced_lab = cv2.merge((l_enhanced, a, b))\n        return cv2.cvtColor(enhanced_lab, cv2.COLOR_LAB2BGR)\n</code></pre> <ol> <li>This is the key line \u2014 CLAHE is applied only to the L (Lightness) channel. The <code>a</code> (green\u2013red) and <code>b</code> (blue\u2013yellow) channels remain untouched, preserving the original parcel colours.</li> </ol>"},{"location":"preprocessing/#effect-comparison","title":"Effect Comparison","text":"Shadows Glare Colours No preprocessing Lost detail Washed out Accurate Global histogram eq Lifted Reduced Distorted \u274c CLAHE on RGB Lifted Reduced Shifted \u274c CLAHE on LAB (SpecularGuard) Lifted \u2705 Stabilised \u2705 Preserved \u2705"},{"location":"preprocessing/#usage","title":"Usage","text":"<pre><code>from src.preprocess.clahe_engine import SpecularGuard\nimport cv2\n\n# Initialise\nguard = SpecularGuard(clip_limit=2.5, tile_grid=[8, 8])\n\n# Process a single image\nimage = cv2.imread(\"data/test1.jpg\")\nenhanced = guard.process(image)\n\n# Save result\ncv2.imwrite(\"output/enhanced.jpg\", enhanced)\n</code></pre> <p>Tuning for Your Environment</p> <ul> <li>Very bright lights (lots of glare): Lower <code>clip_limit</code> to 1.5\u20132.0</li> <li>Very dark conveyor: Raise <code>clip_limit</code> to 3.0\u20134.0</li> <li>Large images: Increase <code>tile_grid</code> to <code>[16, 16]</code> for finer local adaptation</li> </ul>"},{"location":"trainers/","title":"Trainers","text":"<p>Two model architectures, one unified interface.</p>"},{"location":"trainers/#yolov8-seg","title":"\u26a1 YOLOv8-Seg","text":"<p>CNN-based instance segmentation. Grid-based detection, fast inference, high batch sizes. Wraps Ultralytics with hook bridging and dynamic augmentation injection.</p> <p> Read More</p>"},{"location":"trainers/#rf-detr-seg","title":"\ud83e\udd16 RF-DETR-Seg","text":"<p>Transformer-based instance segmentation. DINOv2 backbone, deformable attention, dual learning rates. Global context understanding for overlapping parcels.</p> <p> Read More</p>"},{"location":"trainers/rfdetr/","title":"RF-DETR-Seg Trainer","text":"<p>The <code>RFDETRTrainer</code> wraps Roboflow's RF-DETR Segmentation model \u2014 a transformer-based architecture powered by a DINOv2 backbone \u2014 into the modular <code>BaseTrainer</code> interface.</p> <p> Source: <code>src/training/trainers/rfdetr.py</code> Registry Name: <code>\"rfdetr\"</code></p>"},{"location":"trainers/rfdetr/#what-is-rf-detr","title":"What Is RF-DETR?","text":"<p>RF-DETR (Roboflow Detection Transformer) is a real-time, end-to-end object detection and instance segmentation model. Unlike YOLO's grid-based approach, RF-DETR uses:</p> <ul> <li>DINOv2 as its backbone \u2014 a self-supervised Vision Transformer pretrained on massive data</li> <li>Deformable attention \u2014 allowing the model to attend to relevant image regions directly</li> <li>No NMS required \u2014 end-to-end detection with bipartite matching</li> </ul> Aspect YOLO (CNN) RF-DETR (Transformer) Backbone CSPDarknet DINOv2 (ViT) Detection Grid-based anchors Learnable queries Context Local (convolutional) Global (self-attention) Post-processing NMS Bipartite matching Min VRAM ~4 GB ~8 GB Batch size 16+ 4 (with grad accum)"},{"location":"trainers/rfdetr/#registration","title":"Registration","text":"<pre><code>@TRAINERS.register('rfdetr')\nclass RFDETRTrainer(BaseTrainer):\n</code></pre>"},{"location":"trainers/rfdetr/#constructor","title":"Constructor","text":"<pre><code>def __init__(self, config: dict):\n    super().__init__(config)\n    self.model_size = config.get('model_size', 'm')\n    self.dataset_path = Path(config.get('dataset_path'))\n    self.history = []                                           # (1)!\n</code></pre> <ol> <li>Accumulates per-epoch loss data for plotting training curves after training completes</li> </ol>"},{"location":"trainers/rfdetr/#model-building","title":"Model Building","text":"<pre><code>def build_model(self):\n    if self.model_size == 'm':\n        self.model = RFDETRSegMedium()                          # (1)!\n    else:\n        self.model = RFDETRSegSmall()\n</code></pre> <ol> <li>Unlike YOLO where <code>.pt</code> files are loaded, RF-DETR initialises with pretrained DINOv2 weights automatically</li> </ol> Size Model Class Backbone Use Case <code>m</code> (Medium) <code>RFDETRSegMedium</code> DINOv2-B/14 Best accuracy <code>s</code> (Small) <code>RFDETRSegSmall</code> DINOv2-S/14 Faster, less VRAM"},{"location":"trainers/rfdetr/#training-dual-learning-rates","title":"Training \u2014 Dual Learning Rates","text":"<p>This is the most important concept for transformer training. The DINOv2 backbone was pretrained on millions of images \u2014 you don't want to destroy those learned features with a high learning rate. So RF-DETR uses two separate learning rates:</p> <pre><code>self.model.train(\n    dataset_dir=str(self.dataset_path),\n    output_dir=str(self.output_dir),\n    epochs=epochs,\n    batch_size=batch_size,\n    resolution=resolution,\n\n    lr=optim_cfg.get('lr', 1e-4),              # (1)!\n    lr_encoder=optim_cfg.get('lr_encoder', 1e-5),  # (2)!\n    weight_decay=optim_cfg.get('weight_decay', 1e-4),\n\n    use_ema=tricks.get('use_ema', True),        # (3)!\n    grad_accum_steps=tricks.get('grad_accum_steps', 4),  # (4)!\n\n    early_stopping=es_cfg.get('enabled', True),\n    early_stopping_patience=es_cfg.get('patience', 7),\n\n    log_every_n_steps=1\n)\n</code></pre> <ol> <li>Head LR (1e-4): Higher learning rate for the detection head \u2014 this is the part that learns your specific classes</li> <li>Encoder LR (1e-5): 10\u00d7 lower for the DINOv2 backbone \u2014 fine-tunes gently without destroying pretrained features</li> <li>Exponential Moving Average (EMA): Maintains a smoothed copy of the model weights, reducing oscillation</li> <li>Gradient accumulation: With batch=4 and accum=4, effective batch size = 16. This is critical for transformers needing larger batch sizes but limited by VRAM</li> </ol> <pre><code>graph LR\n    subgraph \"Model Architecture\"\n        A[\"DINOv2 Backbone&lt;br/&gt;lr = 1e-5 \ud83d\udc22\"] --&gt; B[\"Detection Head&lt;br/&gt;lr = 1e-4 \ud83d\udc07\"]\n    end\n    subgraph \"Training Tricks\"\n        C[\"EMA \u2705\"] --&gt; D[\"Smoothed weights\"]\n        E[\"Grad Accum 4\u00d7\"] --&gt; F[\"Effective batch = 16\"]\n    end</code></pre> <p>Why the Low Encoder LR Matters</p> <p>Setting <code>lr_encoder</code> too high (e.g., same as <code>lr</code>) will cause catastrophic forgetting \u2014 the backbone loses its general visual understanding and overfits to your small dataset. The 10\u00d7 gap is a well-established practice in transfer learning.</p>"},{"location":"trainers/rfdetr/#loss-curve-logging","title":"Loss Curve Logging","text":"<p>RF-DETR doesn't have Ultralytics-style callbacks, so the trainer injects its own:</p> <pre><code>def log_metrics_callback(data):\n    self.history.append(data)                               # (1)!\n\nif not hasattr(self.model, 'callbacks'):\n    self.model.callbacks = {\"on_fit_epoch_end\": []}\nself.model.callbacks[\"on_fit_epoch_end\"].append(log_metrics_callback)\n</code></pre> <ol> <li>Each <code>data</code> dict contains <code>epoch</code>, <code>train_loss</code>, <code>test_loss</code> \u2014 accumulated across training</li> </ol> <p>After training completes, the curves are plotted:</p> <pre><code>def _plot_loss_curves(self):\n    df = pd.DataFrame(self.history)\n\n    plt.figure(figsize=(10, 6))\n    if 'train_loss' in df.columns:\n        plt.plot(df['epoch'], df['train_loss'],\n                 label='Train Loss', color='blue')\n    if 'test_loss' in df.columns:\n        plt.plot(df['epoch'], df['test_loss'],\n                 label='Val Loss', color='orange')\n\n    plt.title('RF-DETR Segmentation Training &amp; Validation Loss')\n    plt.xlabel('Epoch')\n    plt.ylabel('Loss')\n    plt.legend()\n    plt.grid(True)\n\n    plot_path = self.output_dir / 'loss_curves.png'\n    plt.savefig(plot_path)\n</code></pre> <p>Tip</p> <p>The loss curves PNG is saved to <code>models/rfdetr/&lt;timestamp&gt;/loss_curves.png</code> alongside the model weights.</p>"},{"location":"trainers/rfdetr/#onnx-export","title":"ONNX Export","text":"<p>RF-DETR only supports ONNX export natively:</p> <pre><code>def export(self, format: str = 'onnx'):\n    if format.lower() != 'onnx':\n        logger.warning(f\"\u26a0\ufe0f RF-DETR only supports ONNX. Ignoring: {format}\")\n\n    self.model.export(\n        output_dir=str(self.output_dir),\n        simplify=True                                       # (1)!\n    )\n</code></pre> <ol> <li>Graph simplification removes redundant operations, making the ONNX model faster for edge deployment</li> </ol> <p>The output file is named <code>inference_model.onnx</code> by Roboflow's convention.</p>"},{"location":"trainers/rfdetr/#dataset-format-coco","title":"Dataset Format \u2014 COCO","text":"<p>RF-DETR expects COCO-format annotations (unlike YOLO's text-based labels):</p> <pre><code>data/rfdetr_dataset/\n\u251c\u2500\u2500 train/\n\u2502   \u251c\u2500\u2500 images/\n\u2502   \u2502   \u251c\u2500\u2500 img001.jpg\n\u2502   \u2502   \u2514\u2500\u2500 ...\n\u2502   \u2514\u2500\u2500 _annotations.coco.json     \u2190 COCO format\n\u251c\u2500\u2500 valid/\n\u2502   \u251c\u2500\u2500 images/\n\u2502   \u2514\u2500\u2500 _annotations.coco.json\n\u2514\u2500\u2500 test/\n    \u251c\u2500\u2500 images/\n    \u2514\u2500\u2500 _annotations.coco.json\n</code></pre> <p>The COCO JSON contains bounding boxes, segmentation polygons, category IDs, and image metadata in a single structured file.</p>"},{"location":"trainers/rfdetr/#configuration-reference","title":"Configuration Reference","text":"configs/optimizers/rfdetr_optim.yaml<pre><code>epochs: 101\nbatch_size: 4                          # Protect 12GB VRAM\nimage_size: 640                        # Must be divisible by 56 for DINOv2\n\noptimizer:\n  type: \"AdamW\"\n  lr: 0.0001                          # 1e-4 for the head\n  lr_encoder: 0.00001                 # 1e-5 for DINOv2 backbone\n  weight_decay: 0.0001\n\ntraining_tricks:\n  use_ema: true                        # Smoothed weights\n  grad_accum_steps: 4                  # 4 batch \u00d7 4 accum = 16 effective\n\nearly_stopping:\n  enabled: true\n  patience: 7\n</code></pre> <p>Image Size Constraint</p> <p>RF-DETR's DINOv2 backbone uses 14\u00d714 patches. The image resolution must be divisible by 56 (14 \u00d7 4 from the multi-scale feature map). 560 and 640 are the most common choices.</p>"},{"location":"trainers/rfdetr/#yolo-vs-rf-detr-when-to-use-which","title":"YOLO vs RF-DETR \u2014 When to Use Which","text":"Scenario Recommended Speed matters (real-time conveyor) YOLO You have lots of training data (5k+) YOLO Objects overlap heavily RF-DETR Small dataset (&lt;1k images) RF-DETR (DINOv2 transfers well) Maximum accuracy needed RF-DETR Limited VRAM (&lt;8 GB) YOLO"},{"location":"trainers/yolo/","title":"YOLOv12-seg Trainer","text":"<p>The <code>YOLOTrainer</code> wraps Ultralytics' YOLOv12 segmentation engine into the modular <code>BaseTrainer</code> interface. It handles model initialisation, dataset configuration, hook bridging, and the full train/eval/export cycle.</p> <p> Source: <code>src/training/trainers/yolo.py</code> Registry Names: <code>\"yolo\"</code>, <code>\"yolov26\"</code></p>"},{"location":"trainers/yolo/#registration","title":"Registration","text":"<pre><code>@TRAINERS.register('yolov26')\n@TRAINERS.register('yolo')\nclass YOLOTrainer(BaseTrainer):\n</code></pre> <p>The dual registration means you can use either <code>model_type: \"yolo\"</code> or <code>model_type: \"yolov26\"</code> in your config. Both resolve to the same class.</p>"},{"location":"trainers/yolo/#constructor-what-happens-on-instantiation","title":"Constructor \u2014 What Happens on Instantiation","text":"<pre><code>def __init__(self, config: dict):\n    super().__init__(config)                                       # (1)!\n    self.model_size = config.get('model_size', 'm')                # (2)!\n    self.dataset_path = Path(config.get('dataset_path',\n                             'data/isi_3k_dataset'))\n    self.data_yaml_path = self._prepare_data_yaml()                # (3)!\n</code></pre> <ol> <li>Calls <code>BaseTrainer.__init__()</code> which creates output dirs, initialises state vars, and attaches hooks</li> <li>Model size determines which pretrained weights to load (<code>yolov12n-seg.pt</code>, <code>yolov12m-seg.pt</code>, etc.)</li> <li>Immediately checks for / generates the <code>data.yaml</code> that YOLO requires</li> </ol>"},{"location":"trainers/yolo/#auto-generated-datayaml","title":"Auto-Generated data.yaml","text":"<p>YOLO needs a <code>data.yaml</code> file to know where images and labels live. Instead of manually creating one, the trainer generates it dynamically from your training config:</p> <pre><code>def _prepare_data_yaml(self) -&gt; str:\n    yaml_path = self.dataset_path / 'data.yaml'\n\n    if not yaml_path.exists():\n        data_dict = {\n            'path': str(self.dataset_path.absolute()),\n            'train': 'images/train',\n            'val': 'images/val',\n            'test': 'images/test',\n            'nc': self.config.get('nc', 2),                         # (1)!\n            'names': self.config.get('class_names',\n                     ['carton', 'polybag'])                         # (2)!\n        }\n        with open(yaml_path, 'w') as f:\n            yaml.dump(data_dict, f, default_flow_style=False)\n\n    return str(yaml_path)\n</code></pre> <ol> <li>Number of classes \u2014 reads directly from <code>train.yaml</code></li> <li>Class name list \u2014 also from <code>train.yaml</code>. This is the single source of truth</li> </ol> <p>Single Source of Truth</p> <p>By generating <code>data.yaml</code> from <code>train.yaml</code>, you never have mismatched class definitions. Change <code>class_names</code> in one place and it propagates everywhere.</p>"},{"location":"trainers/yolo/#model-building","title":"Model Building","text":"<pre><code>def build_model(self):\n    resume_path = self.config.get('resume_path')\n\n    if resume_path:\n        self.model = YOLO(resume_path)       # Resume from checkpoint\n    else:\n        model_name = f\"yolov12{self.model_size}-seg.pt\"\n        self.model = YOLO(model_name)        # Fresh pretrained weights\n</code></pre> <code>model_size</code> Weights File Parameters Speed <code>n</code> <code>yolov12n-seg.pt</code> ~3.4M Fastest <code>s</code> <code>yolov12s-seg.pt</code> ~11.8M Fast <code>m</code> <code>yolov12m-seg.pt</code> ~27.3M Balanced <code>l</code> <code>yolov12l-seg.pt</code> ~46.0M Accurate <code>x</code> <code>yolov12x-seg.pt</code> ~71.8M Most Accurate"},{"location":"trainers/yolo/#hook-bridging-the-key-innovation","title":"Hook Bridging \u2014 The Key Innovation","text":"<p>This is the most interesting part of the trainer. Ultralytics has its own callback system, but IsiDetector has its own hook system defined in <code>BaseTrainer</code>. The trainer bridges between them:</p> <pre><code>def _inject_hooks(self):\n    \"\"\"Bridges Ultralytics callbacks to our BaseTrainer Hooks.\"\"\"\n    def on_train_epoch_end(trainer):\n        self.current_epoch = trainer.epoch                     # (1)!\n\n        if hasattr(trainer, 'tloss') and trainer.tloss is not None:\n            self.current_loss = float(trainer.tloss.sum())     # (2)!\n        else:\n            self.current_loss = 0.0\n\n        self.call_hooks('after_epoch')                         # (3)!\n\n    self.model.add_callback(\"on_train_epoch_end\", on_train_epoch_end)\n</code></pre> <ol> <li>Copy the epoch number from Ultralytics' internal <code>trainer</code> into our <code>BaseTrainer.current_epoch</code></li> <li>Extract the total loss from Ultralytics' PyTorch tensor \u2014 <code>.sum()</code> collapses it to a scalar</li> <li>Now broadcast to our hooks (e.g., <code>IndustrialLogger</code>) which read <code>self.current_epoch</code> and <code>self.current_loss</code></li> </ol> <pre><code>sequenceDiagram\n    participant Ultra as Ultralytics Engine\n    participant Bridge as _inject_hooks callback\n    participant BT as BaseTrainer state\n    participant Hook as IndustrialLogger\n\n    Ultra-&gt;&gt;Bridge: on_train_epoch_end(trainer)\n    Bridge-&gt;&gt;BT: self.current_epoch = trainer.epoch\n    Bridge-&gt;&gt;BT: self.current_loss = trainer.tloss.sum()\n    Bridge-&gt;&gt;Hook: self.call_hooks('after_epoch')\n    Hook-&gt;&gt;BT: Reads trainer.current_epoch, trainer.current_loss\n    Hook-&gt;&gt;Hook: Prints formatted row</code></pre>"},{"location":"trainers/yolo/#training-execution","title":"Training Execution","text":"<p>The <code>train()</code> method assembles everything and launches Ultralytics:</p> <pre><code>def train(self):\n    if self.model is None:\n        self.build_model()\n\n    self._inject_hooks()\n\n    # Extract everything from config\n    epochs = self.config.get('epochs', 300)\n    batch_size = self.config.get('batch_size', 16)\n    img_size = self.config.get('image_size', 640)\n    optim_cfg = self.config.get('optimizer', {})\n    sched_cfg = optim_cfg.get('scheduler', {})\n    es_cfg = self.config.get('early_stopping', {})\n    ckpt_cfg = self.config.get('checkpoint', {})\n\n    # Dynamic augmentation extraction\n    yolo_aug_keys = ['hsv_h', 'hsv_s', 'hsv_v', 'fliplr',\n                     'flipud', 'mosaic', 'scale', 'translate', 'degrees']\n    yolo_kwargs = {k: v for k, v in self.config.items()\n                   if k in yolo_aug_keys}                      # (1)!\n\n    self.call_hooks('before_train')\n\n    # Calculate YOLO's lrf (final LR fraction)\n    lr0 = optim_cfg.get('lr', 0.01)\n    eta_min = sched_cfg.get('eta_min', 0.0001)\n    lrf = (eta_min / lr0) if lr0 &gt; 0 else 0.01                # (2)!\n\n    # Timestamped run folder\n    run_date = datetime.now().strftime(\"%d-%m-%Y\")\n\n    self.model.train(\n        data=self.data_yaml_path,\n        epochs=epochs,\n        batch=batch_size,\n        imgsz=img_size,\n        project=str(base_project_dir),\n        name=run_date,\n        optimizer=optim_cfg.get('type', 'auto'),\n        lr0=lr0,\n        lrf=lrf,                                               # (3)!\n        weight_decay=optim_cfg.get('weight_decay', 0.0005),\n        warmup_epochs=sched_cfg.get('warmup_epochs', 3.0),\n        cos_lr=(sched_cfg.get('type') == 'CosineAnnealing'),\n        patience=es_cfg.get('patience', 50),\n        save_period=ckpt_cfg.get('save_frequency', -1),\n        **yolo_kwargs                                          # (4)!\n    )\n\n    self.call_hooks('after_train')\n</code></pre> <ol> <li>Scans the entire config for YOLO augmentation keys and extracts only the ones present \u2014 so you can add or remove augmentations purely in YAML</li> <li>YOLO's <code>lrf</code> is the ratio of final LR to initial LR. We calculate it from <code>eta_min / lr0</code> so the cosine schedule ends at the right value</li> <li>This connects your YAML scheduler config to YOLO's native cosine annealing</li> <li>Augmentation parameters get splat-injected as keyword arguments</li> </ol>"},{"location":"trainers/yolo/#evaluation","title":"Evaluation","text":"<p>The <code>evaluate()</code> method includes WSL-specific memory management:</p> <pre><code>def evaluate(self) -&gt; dict:\n    # Clean GPU memory before validation spike\n    gc.collect()\n    torch.cuda.empty_cache()\n\n    # Suppress stdout (kills glitchy progress bars)\n    original_stdout = sys.stdout\n    sys.stdout = open(os.devnull, 'w')\n\n    try:\n        results = self.model.val(\n            data=self.data_yaml_path,\n            batch=8,         # Lower than training batch\n            workers=2,       # Prevents RAM spikes on WSL\n            verbose=False\n        )\n    finally:\n        sys.stdout.close()\n        sys.stdout = original_stdout\n</code></pre> <p>After validation, it prints a formatted Executive Summary:</p> <pre><code>\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n              VALIDATION EXECUTIVE SUMMARY\n\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n Metric              | Bounding Box   | Polygon Mask\n-------------------------------------------------------\n mAP @ 50            | 0.8542         | 0.8231\n mAP @ 50-95         | 0.6128         | 0.5934\n-------------------------------------------------------\n Inference Speed      | 12.45 ms per image\n\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\n</code></pre>"},{"location":"trainers/yolo/#configuration-reference","title":"Configuration Reference","text":"train.yamlyolo_optim.yaml <pre><code>model_type: \"yolo\"\nmodel_size: \"m\"\noptimizer_config: \"configs/optimizers/yolo_optim.yaml\"\ndataset_path: \"data/isi_3k_dataset\"\nbatch_size: 16\nimage_size: 640\nmixed_precision: true\n\n# Augmentations (all optional)\nhsv_h: 0.015\nhsv_s: 0.7\nhsv_v: 0.4\nfliplr: 0.5\nmosaic: 1.0\nscale: 0.5\n</code></pre> <pre><code>epochs: 200\n\noptimizer:\n  type: \"AdamW\"\n  lr: 0.001\n  weight_decay: 0.0005\n  scheduler:\n    type: \"CosineAnnealing\"\n    warmup_epochs: 3\n    T_max: 200\n    eta_min: 0.00001\n\nearly_stopping:\n  enabled: true\n  patience: 10\n  min_delta: 0.0005\n  monitor: \"mAP50\"\n\ncheckpoint:\n  save_best_only: true\n  save_frequency: 5\n</code></pre>"},{"location":"web-app/","title":"IsiDetector VisionAI Platform","text":"<p>The Web Application provides a modern, responsive interface for managing your Industrial VisionAI pipeline without using the command line.</p>"},{"location":"web-app/#system-overview","title":"System Overview","text":"<p>The platform is built using: - Backend: Python Flask (Robust, lightweight). - Inference Engine: Asynchronous <code>StreamHandler</code> for non-blocking AI processing. - Communication: MJPEG Streaming for low-latency visual feedback. - Frontend: Clean Material Design with responsive analytics.</p>"},{"location":"web-app/#main-navigation","title":"Main Navigation","text":"Section Purpose Live Inference Real-time visual monitoring and control. Analytics Historical data exploration and chart views. Models High-level overview of active perception weights. Settings Configuration of thresholds and line triggers."},{"location":"web-app/#internal-architecture","title":"Internal Architecture","text":"<p>The web app is split into two decoupled Python modules:</p> <pre><code>isitec_app/\n\u251c\u2500\u2500 app.py              # Flask routes \u2014 thin HTTP layer only\n\u2514\u2500\u2500 stream_handler.py   # All AI and streaming logic\n    \u251c\u2500\u2500 StreamHandler   # Session manager (start/stop, stats, language)\n    \u251c\u2500\u2500 LiveReader      # Background video-capture thread (zero-lag queue)\n    \u2514\u2500\u2500 TelemetryPublisher  # Simulated MQTT publisher to isitec/sorting\n</code></pre> <p>Key design principle: <code>app.py</code> never touches OpenCV or model weights directly \u2014 it delegates everything to <code>StreamHandler</code>. This keeps Flask routes clean and thread-safe.</p>"},{"location":"web-app/#async-processing-pipeline","title":"Async Processing Pipeline","text":"<pre><code>graph LR\n    A[Camera / RTSP / File] --&gt; B[LiveReader&lt;br/&gt;Background Thread]\n    B --&gt;|Latest frame| C[_inference_loop&lt;br/&gt;Daemon Thread]\n    C --&gt; D[VisionEngine&lt;br/&gt;ByteTrack + Line Count]\n    D --&gt; E[latest_annotated&lt;br/&gt;JPEG buffer]\n    E --&gt; F[/video_feed&lt;br/&gt;MJPEG stream]\n    D --&gt;|line_crossed event| G[TelemetryPublisher&lt;br/&gt;MQTT log]</code></pre> <p>The <code>LiveReader</code> uses a <code>queue.Queue(maxsize=1)</code> \u2014 always discarding stale frames \u2014 so the inference thread always processes the most recent camera frame, not a backlog.</p>"},{"location":"web-app/#heartbeat","title":"Heartbeat","text":"<p>A lightweight daemon thread runs every 10 seconds to keep the process alive on platforms that may reclaim idle worker processes. It performs no I/O and has no observable side effects.</p>"},{"location":"web-app/#deployment","title":"Deployment","text":"<p>The web app is accessible internally on port 9501.</p> <pre><code># To start the platform\npython isitec_app/app.py\n</code></pre> <p>It can also be deployed via Docker for industrial servers: <pre><code>docker build -t isitec-platform ./isitec_app\ndocker run -p 9501:9501 isitec-platform\n</code></pre></p>"},{"location":"web-app/functionalities/","title":"Platform Functionalities","text":"<p>Deep-dive into the core features of the IsiDetector Web App.</p>"},{"location":"web-app/functionalities/#1-dual-mode-perception","title":"1. Dual-Mode Perception","text":"<p>The platform supports two distinct perception modes reachable via the \"Modes\" dropdown:</p> <ul> <li>Mode 1 (YOLO): Speed-optimized detection using the YOLOv12-seg architecture. Best for high-speed conveyor belts.</li> <li>Mode 2 (RF-DETR): Transformer-based detection. Highly accurate for complex, overlapping objects or varying light conditions.</li> </ul>"},{"location":"web-app/functionalities/#2-intelligent-source-switching","title":"2. Intelligent Source Switching","text":"<p>The app automatically adjusts its processing logic based on the input source:</p>"},{"location":"web-app/functionalities/#local-processing-inspection-mode","title":"Local Processing (Inspection Mode)","text":"<ul> <li>Images: When you upload a JPG or PNG, the system processes it once and \"freezes\" the result on the screen. This is perfect for quality control sub-stations.</li> <li>Videos: Processes the MP4 file frame-by-frame, allowing you to watch the \"replay\" of an event with AI overlays.</li> </ul>"},{"location":"web-app/functionalities/#live-telemetry-streaming-mode","title":"Live Telemetry (Streaming Mode)","text":"<ul> <li>USB/Integrated Cameras: Connects directly to local hardware with near-zero latency.</li> <li>RTSP Streams: Connects to industrial IP cameras (e.g., Hikvision, Amcrest) using the <code>rtsp://</code> protocol.</li> </ul>"},{"location":"web-app/functionalities/#3-dynamic-configuration","title":"3. Dynamic Configuration","text":"<p>Unlike static applications, the web app is \"Config-Live\". It reads the <code>configs/train.yaml</code> file every time a stream starts.</p> <p>This allows you to change: - Confidence Cutoffs: <code>conf_threshold</code> - Line Positions: Logic-based positioning. - Tracker Memory: <code>track_buffer</code> (How many frames to \"remember\" a parcel if it's hidden).</p>"},{"location":"web-app/functionalities/#4-real-time-analytics","title":"4. Real-Time Analytics","text":"<p>The Detection Analytics chart updates dynamically as parcels cross the line. - Live Filter: Shows the current session's productivity. - Historical (24h/7d/30d): Aggregates CSV logs to show long-term productivity trends.</p>"},{"location":"web-app/functionalities/#5-global-language-switching","title":"5. Global Language Switching","text":"<p>The platform supports real-time translation of the user interface. By hitting the <code>/api/language</code> endpoint (or toggling the UI switch), the <code>StreamHandler</code> adjusts its internal reporting and the frontend updates labels instantly without a page refresh.</p> <p>Supported locales include: - English (EN) - French (FR) - German (DE)</p>"},{"location":"web-app/functionalities/#6-backend-api-fast-integration","title":"6. Backend API (Fast Integration)","text":"<p>For industrial control systems (SCADA/PLC) that need to interact with IsiDetector externally, the following REST APIs are available:</p> Endpoint Method Payload / Params Description <code>/video_feed</code> <code>GET</code> \u2014 MJPEG stream (<code>multipart/x-mixed-replace</code>). Embed in <code>&lt;img src=\"/video_feed\"&gt;</code>. <code>/api/start</code> <code>POST</code> <code>{\"source\": \"...\", \"model_type\": \"yolo\\|rfdetr\", \"weights\": \"path/to/weights\"}</code> Starts the inference loop. <code>weights</code> is optional \u2014 falls back to the latest found checkpoint. <code>/api/stop</code> <code>POST</code> \u2014 Gracefully terminates the stream and flushes the session CSV. <code>/api/upload</code> <code>POST</code> <code>Multipart/Form-Data</code> (field: <code>file</code>) Uploads a JPG/PNG/MP4 to <code>isitec_app/uploads/</code> for local inspection. Returns <code>{\"filepath\": \"...\"}</code>. <code>/api/language</code> <code>POST</code> <code>{\"language\": \"en\\|fr\\|de\"}</code> Switches the StreamHandler locale in real-time without a page reload. <code>/api/stats</code> <code>GET</code> \u2014 Returns <code>{\"is_running\": bool, \"counts\": {...}, \"last_detected\": {...}}</code>. <code>/api/chart</code> <code>GET</code> <code>?period=live\\|24h\\|7d\\|30d</code> <code>live</code> mirrors <code>/api/stats</code> counts. Historical periods aggregate CSV logs from <code>isitec_app/logs/</code>. <p>Chart period detail</p> <ul> <li><code>live</code> \u2014 mirrors the current in-memory session counts (no disk read)</li> <li><code>24h</code> / <code>7d</code> / <code>30d</code> \u2014 scans all <code>.csv</code> files in <code>isitec_app/logs/</code> and counts rows whose timestamp falls within the cutoff window</li> </ul>"}]}