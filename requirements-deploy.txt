# IsiDetector — Deploy-only dependencies (inference + web app)
# Used by the production Dockerfile. Does NOT include training packages.

# ── Web framework ────────────────────────────────────────────────────────────
Flask>=3.0.0
Werkzeug>=3.0.0

# ── Computer vision ──────────────────────────────────────────────────────────
opencv-python-headless>=4.8.0
supervision>=0.27.0
numpy>=1.24.0
Pillow>=10.0.0

# ── Model runtimes (.pt native inference) ────────────────────────────────────
# torch and torchvision are installed separately via --index-url for CUDA builds
# rfdetr runs in its own container (Dockerfile.rfdetr) due to incompatible deps
ultralytics>=8.4.0
requests>=2.30.0

# ── Optimized runtimes (.onnx / .xml) ───────────────────────────────────────
# onnxruntime-gpu is installed separately for CUDA builds; CPU-only uses onnxruntime
openvino>=2024.0.0

# ── Hardware monitoring ──────────────────────────────────────────────────────
nvidia-ml-py3>=7.352.0
psutil>=5.9.0

# ── Config ───────────────────────────────────────────────────────────────────
PyYAML>=6.0
